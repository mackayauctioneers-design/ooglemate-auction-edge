# Memory: architecture/identity/governance-rule-v1
Updated: now

## Iron Rule: Identity Is Backend-Only

Vehicle identity resolution (make, model, variant, platform classification, badge extraction) is defined EXCLUSIVELY in:

1. `supabase/functions/_shared/taxonomy/normalizeVehicleIdentity.ts` — canonical normalizer
2. `supabase/functions/_shared/taxonomy/derivePlatform.ts` — platform classifier + badge extractor
3. `supabase/functions/_shared/taxonomy/parseSlug.ts` — MULTI_WORD_MODELS for slug parsing
4. DB taxonomy tables (`taxonomy_models`, `taxonomy_variant_rank`)
5. DB function `derive_platform_class(make, model)` — SQL mirror

## What Was Removed (2026-03-02)

| File | What Was Killed | Replacement |
|------|----------------|-------------|
| `src/utils/variantFamilyExtractor.ts` | 261-line hardcoded variant ladders + VARIANT_FAMILIES map | Stripped to display-only (isPicklesSource, getKmStatus) |
| `score-operator-opportunities/index.ts` | Inline `derivePlatform()` + `extractBadge()` copies | Imports from `_shared/taxonomy/derivePlatform.ts` |
| `listing-normalizer/index.ts` | Inline `extractMake()` + `extractModel()` with hardcoded `modelMap` | Removed entirely; normalizer is sole identity source |
| `pickles-replication-cron/index.ts` | Inline `derivePlatform()` + `extractBadge()` copies | Imports from `_shared/taxonomy/derivePlatform.ts` |
| `caroogle-shadow-promotion/index.ts` | Inline `derivePlatform()` + `extractBadge()` copies | Imports from `_shared/taxonomy/derivePlatform.ts` |
| `caroogle-shadow-cron/index.ts` | Inline `extractBadge()` copy, renamed `extractModel` to `extractRawModelFromApi` | Imports badge from shared; raw model extractor is API-specific, not identity |
| `pickles-ingest-cron/index.ts` | 24-line inline badge if/else chain | Replaced with `canonicalExtractBadge()` from shared |
| `src/utils/derivePlatform.ts` | Standalone copy | Now explicitly marked as display-only mirror of backend |

## Legacy Shim (Marked for Removal)

`src/types/index.ts` → `extractVariantFamily_UI_ONLY_DEPRECATED()` — renamed from `extractVariantFamily`. Still used by `picklesAlertMatcher.ts`, `ManualFingerprintForm.tsx`, `googleSheetsService.ts` via deprecated alias. Must be replaced by reading `variant_family` from DB.

## Test Harnesses

1. **Regression tests** (`identity-regression-tests/index_test.ts`) — 19 tests: platform separation, badge isolation, priority, null handling
2. **Contract tests** (`identity-contract-tests/index_test.ts`) — 22 tests: 20 real-world fixtures from live DB data + 2 cross-collision assertions

## Remaining Acceptable Patterns

- `ooglebot/index.ts` → `MODEL_TO_MAKE` — fallback for LLM intent parsing, not identity resolution
- `useBuyAgainTargets.ts` → `.includes("GX")` — reads from DB-resolved fields, display bucketing only
- `LogSalePage.tsx` — inline badge list for form UI hints only

## Frontend Rule

Frontend may DISPLAY `vehicle.variant_family`, `vehicle.platform_class`, etc.
Frontend must NEVER derive, classify, or resolve identity.
